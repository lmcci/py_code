# 分割字符串 如果不传值 就以空白字符（空格 \t \n \r）作分割 返回一个列表 还能指定一个最多分割数量
# test_str = "hello world"
# print(test_str.split("wo"))

# 拼接字符串 用拼接字符串join 传入一个列表 返回一个字符串是列表中所有值加上拼接字符串

test_list = ['a', 'b', 'c']
print("\t".join(test_list))
